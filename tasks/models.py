from django.db import models

# For this assessment, tasks are handled as in-memory JSON objects,
# so we do not define a persistent Task model here.
# This file exists to keep the Django app structure complete.
